import 'package:flutter/material.dart';

import '../../constance/my_theme.dart';

class SettingsScreen extends StatelessWidget {
  const SettingsScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Column(
      children: [
        Text("Setting")
      ],
    );
  }
}
